#include <iostream>
#include <cstdio>
using namespace std;

int main() {
  freopen("billboard.in", "r", stdin);
  freopen("billboard.out", "w", stdout);
  int a1, a2, a3, a4, b1, b2, b3, b4, c1, c2, c3, c4;
  cin >> a1 >> a2 >> a3 >> a4;
  cin >> b1 >> b2 >> b3 >> b4;
  cin >> c1 >> c2 >> c3 >> c4;
  int area;
  int leftblocked;
  
  cout << area;
}
  
  // 1st is area formula with an x1, y1, x2, y2; then you are going to use a math.min, math.max



